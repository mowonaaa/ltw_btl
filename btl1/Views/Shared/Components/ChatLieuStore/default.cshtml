@model IEnumerable<ChatLieu>
@foreach (var item in Model)
{
	<div class="input-checkbox">
		<input type="checkbox" id="category-1">
		<label for="category-1">
			<span></span>
			@item.TenCl
			<small></small>
		</label>
	</div>
}